<!--
  READ-ONLY: scenes run against PRODUCTION Supabase in CI with only the
  publishable (anon) key. Do NOT add logins, form submits, or any action
  that mutates data. See .claude/rules/scenetest.md.
-->

# colors page renders

visitor:

- openTo /colors
- see Soft color
